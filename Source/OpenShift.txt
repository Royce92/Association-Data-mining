 openshift is a cloud computing platform as a service product from red hat  a version for private cloud is named openshift enterprise      the software that runs the service is open sourced under the name openshift origin  and is available on github  developers can use git to deploy web applications in different languages on the platform     openshift also supports binary programs that are web applications  so long as they can run on red hat enterprise linux  this allows the use of arbitrary languages and frameworks  openshift takes care of maintaining the services underlying the application and scaling the application as needed             openshift supports web application frameworks by supporting each language s preferred web integration api  with no required changes to the actual framework code     some frameworks that work unmodified on openshift include  